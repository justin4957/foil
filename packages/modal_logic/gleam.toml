name = "modal_logic"
version = "0.1.0"

description = "Core domain types for modal logic analysis - shared types for propositions, arguments, and formalizations"
licences = ["MIT"]
repository = { type = "github", user = "justin4957", repo = "foil" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
gleam_json = ">= 2.0.0 and < 3.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
