name = "analyst"
version = "0.1.0"

description = "Modal logic analysis system integrating LLM translation and Z3 validation"
licences = ["MIT"]
repository = { type = "github", user = "justin4957", repo = "foil" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
gleam_json = ">= 2.0.0 and < 3.0.0"
gleam_erlang = ">= 0.30.0 and < 1.0.0"

# Local workspace dependencies
anthropic_gleam = { path = "../../packages/anthropic_gleam" }
z3_gleam = { path = "../../packages/z3_gleam" }
modal_logic = { path = "../../packages/modal_logic" }

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
